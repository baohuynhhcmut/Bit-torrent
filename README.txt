# BitTorrent Implementation

This project is an implementation of the BitTorrent protocol, which includes both client and server functionalities. It allows users to download and seed torrent files, offering a full peer-to-peer file-sharing experience.

## Project Structure

## Installation

To install the project dependencies, run the following command:

```sh
npm install